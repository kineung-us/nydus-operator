apiVersion: v1
kind: Service
metadata:
  name: {{ include "nydus.fullname" . }}
  labels:
    {{- include "nydus.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nydus.selectorLabels" . | nindent 4 }}
