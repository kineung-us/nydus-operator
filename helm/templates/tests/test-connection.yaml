apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nydus.fullname" . }}-test-connection"
  labels:
    {{- include "nydus.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nydus.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
